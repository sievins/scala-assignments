package patmat

import common._
import Huffman._

object worksheet {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(81); val res$0 = 
  Nil.length;System.out.println("""res0: Int = """ + $show(res$0))}
}
